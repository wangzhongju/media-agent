#!/bin/sh
set -e

case "$1" in
    remove|upgrade|deconfigure)
        echo "[media-agent] prerm: $1"
        if command -v systemctl >/dev/null 2>&1; then
            systemctl stop media-agent.service || true
        fi
        ;;
    *)
        ;;
esac

exit 0
